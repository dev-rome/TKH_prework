const numbers = [1, 45, 32, 21, 5, 17, 43, 93];
const multiply = 7;

// loops thru mubers array then multiply the array by 7 then prints to console
for (let i = 0; i < numbers.length; i++) {
  console.log(numbers[i] * multiply);
}
